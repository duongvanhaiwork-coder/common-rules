# Frontend Implement UI

Implement the frontend task end-to-end with UX, state, and safety checks.

Requirements:

- Keep component boundaries clear; extract reusable logic into hooks/utilities when needed.
- Handle loading, empty, success, and error states explicitly for async flows.
- Keep accessibility baseline (labels, semantic controls, keyboard interactions).
- Keep API data handling centralized per project conventions.
- Do not run destructive commands unless explicitly requested.
- Do not commit or push unless explicitly requested.
- Add/update tests for meaningful UI logic and critical state transitions.
- Run relevant checks (lint/test/build) and report verification status.

Return:

- What changed
- UX/state impact
- Files touched
- Verification results and remaining risks
